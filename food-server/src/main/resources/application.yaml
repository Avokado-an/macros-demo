server:
  port: 8092

eureka:
  client:
    server-url:
      default-zone: http://localhost:8761/eureka

logstash:
  host: localhost

spring:
  application:
    name: food-server
  data:
    mongodb:
      authentication-database: admin
      username: root
      password: password
      database: food
      port: 27017
      host: localhost
      auto-index-creation: true